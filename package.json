{
  "name": "boolean-ts",
  "version": "1.0.2",
  "description": "Compile time support for runtime logic operations in Typescript",
  "main": "dist/index.js",
  "repository": "https://github.com/waynevanson/boolean-ts",
  "author": "Wayne Van Son",
  "license": "MIT",
  "private": false,
  "files": [
    "dist/",
    "package.json",
    "readme.md"
  ],
  "scripts": {
    "watch": "jest --watchAll",
    "test": "jest",
    "build": "tsc --project ./tsconfig.build.json",
    "prepublishOnly": "yarn test && yarn build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.6"
  }
}
